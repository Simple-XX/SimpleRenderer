
/**
 * @file config.h
 * @brief 项目配置
 * @author Zone.N (Zone.Niuzh@hotmail.com)
 * @version 1.0
 * @date 2023-08-24
 * @copyright MIT LICENSE
 * https://github.com/Simple-XX/SimpleRenderer
 * @par change log:
 * <table>
 * <tr><th>Date<th>Author<th>Description
 * <tr><td>2023-08-24<td>Zone.N<td>创建文件
 * </table>
 */

#ifndef SIMPLERENDER_CONFIG_H
#define SIMPLERENDER_CONFIG_H

#include <string>
#include <string_view>

/// obj 文件目录
static const std::string OBJ_FILE_PATH =
    std::string("@OBJ_FILE_PATH@");

/// 线程数
static constexpr const size_t NPROC = @NPROC@;

/// @name 默认大小
/// @{
static constexpr const size_t WIDTH = 1920;
static constexpr const size_t HEIGHT = 1080;
/// @}

/// 日志文件路径
static const std::string LOG_FILE_PATH =
    std::string("@LOG_FILE_PATH@");
/// 日志文件大小
static constexpr const size_t LOG_FILE_MAX_SIZE = @LOG_FILE_MAX_SIZE@;
/// 日志文件数量
static constexpr const size_t LOG_FILE_MAX_COUNT = @LOG_FILE_MAX_COUNT@;

#endif /* SIMPLERENDER_CONFIG_H */
