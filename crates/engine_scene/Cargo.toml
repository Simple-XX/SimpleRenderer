[package]
name = "engine_scene"
version = "0.1.0"
edition = "2021"
rust-version = "1.73"

[dependencies]
engine_core = { path = "../engine_core" }
engine_render_sw = { path = "../engine_render_sw" }
glam = "0.29"
hecs = "0.10"
